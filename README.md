# 🛒 Shopping List

Aplicativo simples de **lista de compras**, desenvolvido com **TypeScript**, **Bootstrap**, **CSS personalizado** e empacotado com **Vite**.

Permite adicionar, editar, marcar como comprado e remover itens, com armazenamento persistente via `localStorage`.

... (Conteúdo completo como acima)